# HEURISTIC Submission Example
# Duplicate repo test

We want to perform a sequence-to-new-repo into a custom repo named 'custom-repo5'.
Ensure we use this new repo name.

We only need two simple steps in the sequence. We can do 5 minutes between steps to keep things quick.
